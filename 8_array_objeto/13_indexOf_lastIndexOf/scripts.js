let numeros = [5, 6, 2, 4, 9, 6, 2];

console.log(numeros.indexOf(2)); // retorna a posição da primeira ocorrência do elemento no array
console.log(numeros.lastIndexOf(2)); // retorna a posição da última ocorrência do elemento no array