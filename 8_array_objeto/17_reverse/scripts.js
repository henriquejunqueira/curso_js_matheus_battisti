let carros = ['BMW', 'Fiat', 'VW', 'Renault', 'Audi'];
let numeros = [1, 2, 3, 45, 6, 7, 7];

// Inverte a saída de dados exibindo o elemento da última posição primeiro, e o primeiro por último
console.log(carros.reverse());
console.log(numeros.reverse());
