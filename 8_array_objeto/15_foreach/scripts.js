let nomes = ['Henrique', 'Maria', 'José', 'Pedro', 'João'];

nomes.forEach(nome => {
    console.log("O nome é: " + nome);
});

let frutas = ['Banana', 'Uva', 'Maçã', 'Abacaxi'];

frutas.forEach(fruta => {
    console.log(`Fruta: ${fruta}`);
})

// nomes[i] 